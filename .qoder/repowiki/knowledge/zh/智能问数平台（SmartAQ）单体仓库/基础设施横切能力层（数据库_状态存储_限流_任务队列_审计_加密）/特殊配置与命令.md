服务启动需先调用 `initSchema()` 完成建库建表与种子数据；若启用 Redis 需在启动后调用 `warmStateStore()` 预热连接；可通过环境变量 `APP_POOL_MAX`/`EXPECTED_CONCURRENT_USERS` 控制连接池大小，`TASK_WORKER_CONCURRENCY`/`TASK_QUEUE_USER_MAX`/`TASK_HEARTBEAT_TIMEOUT_MS`/`TASK_EXEC_TIMEOUT_MS` 控制任务队列行为，`RATE_LIMIT_MAX`/`USER_QUERY_RATE_MAX` 控制限流阈值。