Express + TypeScript（server.ts 及 server/*）、React + Vite（src/*）、MySQL + Redis（server/infra/db.ts、stateStore.ts）、Prometheus/Grafana/Alertmanager（deploy/）、Playwright E2E（playwright.config.ts）、Docker + docker-compose（Dockerfile、docker-compose.*.yml）。