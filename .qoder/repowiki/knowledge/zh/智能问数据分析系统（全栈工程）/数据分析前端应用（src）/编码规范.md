- 组件按业务域在 components 下分目录组织，每个目录对应一个能力域（如 flexquery、query、reports）。
- 全局状态集中在 hooks 中通过 zustand store 暴露，并由 useXxxStore 形式的 Hook 供组件消费。
- 跨页面复用逻辑以 utils 下的纯函数或 Hook 形式提供，测试文件与实现文件同名并列放置。
- HTTP 请求统一通过 src/api/client.ts 发起，避免在各组件中直接调用 fetch。