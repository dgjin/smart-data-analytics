- 每个业务域以独立子目录组织，内部按功能拆分为多个 .tsx 文件并配套同名的 *.test.tsx 单元测试
- 可复用的可视化能力集中在 charts 目录，其他域通过 props 传入数据与配置来渲染图表，避免重复实现
- 页面级入口组件统一由 shared_ui 中的 Header/Sidebar/ErrorBoundary 包裹，保证全局布局与异常捕获一致
- 对外暴露的组件以默认导出函数形式提供，并通过显式 props 类型定义契约，不直接依赖兄弟域的内部实现