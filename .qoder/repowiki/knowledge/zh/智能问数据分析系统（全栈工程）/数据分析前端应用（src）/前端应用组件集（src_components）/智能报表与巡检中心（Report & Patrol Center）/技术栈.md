React + TypeScript 组件；图标使用 lucide-react；UI 基于 Tailwind CSS；异步任务通过 `pollTask`/`downloadTaskResult` 轮询后端 worker；报表导出由服务端 ReportLab（PDF）、pptxgenjs（PPT）、exceljs/docx（Excel/Word）处理；异常检测使用本地规则引擎 `scanReportForAnomalies`（Z-Score/阈值）。