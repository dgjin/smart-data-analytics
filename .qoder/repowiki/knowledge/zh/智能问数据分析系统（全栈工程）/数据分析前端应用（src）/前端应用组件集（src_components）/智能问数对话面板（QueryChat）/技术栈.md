React + TypeScript 函数组件；UI 使用 Tailwind CSS 类名与 lucide-react 图标；网络层统一通过 `../../api/client` 的 `apiFetch`；SSE 流式消费委托给 `../../utils/sseStream.readSseStream`；SQL 血缘解析使用 `../../utils/sqlLineage.parseSqlLineage`。