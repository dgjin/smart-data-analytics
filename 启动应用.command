#!/bin/bash
# 双击启动入口（Finder）——等价于终端执行 ./start.sh
cd "$(dirname "$0")"
./start.sh
echo ""
echo "------------------------------------------------------------"
read -n 1 -s -r -p "按任意键关闭本窗口（应用已在后台持续运行）..."
