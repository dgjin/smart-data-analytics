#!/bin/bash
# 智能问数据分析系统 · macOS 双击启动入口
# Finder 双击本文件即等价于终端执行 ./start.sh
# 首次双击若被 Gatekeeper 拦截：右键 → 打开 → 打开
cd "$(dirname "$0")"
./start.sh
echo ""
echo "------------------------------------------------------------"
read -n 1 -s -r -p "按任意键关闭本窗口（应用已在后台持续运行）..."
