# 智能问数分析系统 · 功能说明书

> 本文档是系统的**单一事实源功能说明书**（面向开发与评审）。每次系统功能新增或变更时，必须同步更新本文件与《用户使用指南》，并递增「变更记录」章节。系统内置的「帮助」入口实时读取《用户使用指南》展示给终端用户。

- **适用版本**：NL2SQL Pro v0.9.9
- **文档维护约定**：功能变更 → 同步修改本文件 → 更新「变更记录」→ 重新验证

---

## 1. 系统概述

智能问数分析系统是一个面向企业的**自然语言数据分析平台**（NL2SQL）。用户用中文提问，系统自动理解语义、匹配数据表结构、生成并执行 SQL，再将真实查询结果转化为图表、KPI 与洞察解读，最终可一键产出高管决策简报。

核心能力：

- **多源数据集成**：接入 MySQL / PostgreSQL / Greenplum / CSV / API / JSON 等数据源
- **自然语言交互**：中文提问自动转 SQL（NL2SQL），支持歧义澄清
- **自动可视化**：根据查询结果智能推荐图表并输出归因诊断
- **决策简报**：一键自动生成面向高管的分析报告（可导出 PDF）
- **企业级安全**：角色权限、输入防护、限流、审计、敏感列过滤

---

## 2. 角色与权限

系统内置三种角色，登录后可见功能按角色裁剪。

| 角色 | 标识 | 智能问数 | 报表/看板 | 数据源与 Schema | 系统管理 | 知识库/样例/技能 |
|------|------|:---:|:---:|:---:|:---:|:---:|
| 管理员 | `ADMIN` | ✅ | ✅ | ✅ | ✅ | ✅（可审核分享） |
| 分析师 | `ANALYST` | ✅ | ✅ | ❌ | ❌ | ✅（个人维度） |
| 只读 | `VIEWER` | ❌ | ✅ | ❌ | ❌ | ❌ |

- 登录入口：`/` 页面，默认管理员账号 `admin / admin123`（首次部署请务必修改）。
- 越权访问会被前端路由守卫重定向到「决策数据看板」。
- 数据源表结构（Schema 明细）仅管理员可见；非管理员仅能看到表数量。

---

## 3. 功能模块详解

### 3.1 智能问答与查询（NL2SQL）

系统核心功能，位于左侧导航「智能问答与查询」。

**使用流程**

1. 在顶部选择当前数据源（Header 数据源切换器）。
2. 在输入框用中文提问，例如「各类型客户的数量是多少？」。
3. 提交后系统流式返回处理阶段，最终给出 SQL、数据表、图表与解读。

**关键能力**

- **SSE 流式进度**：请求以 `stream:true` 提交，服务端通过 Server-Sent Events 推送阶段事件：
  - `understanding` 正在理解问题语义并匹配数据字段
  - `introspecting` 数据自省中，确认真实取值
  - `executed` SQL 已执行，返回行数
  - `analyzing` 正在基于真实数据生成洞察
- **歧义澄清**：当问题存在多种理解时，系统不直接执行 SQL，而是返回澄清问题与候选理解，由用户确认后重新提交。
- **语义缓存**：相同/等价问题（归一化后）在 10 分钟内命中缓存，秒级返回，审计状态记为 `CACHE`。
- **few-shot 样例注入**：系统从 SQL 样例库挑选与当前问题最相似的「问题-SQL」对，以 user/assistant 消息对形式注入上下文，提升生成准确率。
- **数据自省**（可选）：开启后，模型可先执行一条探查 SQL 确认真实取值，再据此生成最终 SQL。详见 3.6。
- **推导过程记录与可视化**：每次问数全程埋点（traceId），步骤含圈表、知识库命中、自省、SQL 生成/重试、执行、阶段二解读（各带 SQL、行数、耗时）；查询中展示横向步骤器，完成后可在消息内展开「推导过程」垂直时间线回放；`GET /api/query/trace/:traceId` 仅本人或 ADMIN 可查。
- **计划模式**（可选开关，持久化）：开启后提问先由 LLM 生成分析计划（编号步骤/涉及表，不执行），展示计划卡片供批准；批准后携带 planId 提交执行，计划 10 分钟有效、一次性消费，过期/篡改/问题不匹配返回 409。
- **深度分析（中间表清洗链）**（自动触发 + 可选开关强制）：复杂问题先经复杂度评估，多步骤清洗（去重/标准化/过滤/聚合）逐步在源库 SELECT 并物化为应用库物理中间表 `ait_*`（注册表登记，TTL 24 小时，每用户最多 10 张，写入前剔除敏感列），最终 SQL 仅引用中间表时改在应用库执行；清洗链失败不阻断主链路。仅 mysql/postgresql/greenplum 数据源启用。
- **反馈与自学习**：对结果点赞（UP）会自动将该问答沉淀进 SQL 样例库（auto_train），点踩（DOWN）记录负反馈。
- **模型自选**：输入框旁提供模型下拉（目录来自 `GET /api/system/models`，Ollama 实时列已安装模型），选择随提问生效并持久化到浏览器；不同模型的结果缓存互相隔离。
- **降级兜底**：当 LLM/数据库不可用时，返回带明确标识的示例数据，保证界面不白屏。

### 3.2 可视化决策报表

一键自动生成面向高管的分析简报，位于「可视化决策报表」。

- 自动生成执行摘要、关键指标、图表与结论建议；数据库型数据源走真实数据双阶段（查询计划 → 真实执行 → 真实数据摘要撰写）。
- **报告计划模式**（可选开关，持久化）：开启后先由 LLM 生成报表查询计划（每图标题/目的/SQL，不执行），批准后携带 reportPlanId 生成；计划 10 分钟有效、一次性消费，过期/越权/数据源或模板不匹配返回 409。
- **下载 PPT**：服务端 pptxgenjs 组装 PPTX（封面/高管摘要/KPI 页/每图一页/结论页），图表由前端 SVG 转 base64 PNG 提交（`POST /api/report/export`，独立 20MB body 限额）。
- **导出 PDF**（v0.5.3 起改用 ReportLab）：服务端 Python ReportLab 原生排版（矢量文本、非截图），A4 竖版/横版可选，中文内置 CID 字体无乱码，内容完整无错位遮挡；图表由前端 ECharts 转 base64 PNG 提交（`POST /api/report/export-pdf`）；另支持打印友好模式（window.print）。
- **图表同/环比对比**：柱/折线/面积图工具栏与报表全局开关支持同比（YoY）/环比（MoM）基线对比与差异百分比标记；仅时间序列维度（年/月/日/季度等按序排列）可用，机构名等分类维度自动禁用，避免跨维度错配对比。
- **图表点击下钻**（P2-2）：live 链路的柱状/折线/面积图支持点击某个维度值下钻明细——后端基于该图表的原聚合 SQL（AST 提取 FROM/JOIN/WHERE，去聚合改 `SELECT *`，追加 `维度列 = 点击值`，LIMIT 50）真实执行并返回最多 50 行明细（`POST /api/query/drill`），行级权限仍强制注入；仅 ADMIN/ANALYST 可用。
- 支持对图表添加评论与批注（ChartCommentSection）。

### 3.3 决策数据看板

固化指标图表与看板展示（CustomDashboard），适合日常巡检与大屏投放，所有角色可见。

### 3.4 数据源与 Schema（管理员）

位于「数据源与 Schema」，包含多个子页签：

| 子页签 | 功能 |
|--------|------|
| 数据源管理 | 新增/编辑/删除数据源，测试连接，启停智能问数 |
| Schema 查看器 | 查看表结构、字段、类型、行数 |
| 数据血缘 | 展示数据流向与依赖关系（DataLineageView） |
| 知识库 | 维护业务术语、口径、指标定义，供问数时检索 |
| SQL 样例库 | 维护「问题-SQL」训练样例（Training Data） |

**数据源类型**：MySQL、PostgreSQL、Greenplum、CSV、API、JSON。

**数据自省开关**：在数据源卡片上以紫色扫描图标切换（`allowIntrospection`），默认关闭。开启后该数据源的问数允许执行探查 SQL。

**语义指标层**（P1-1）：管理员按数据源登记结构化业务指标（名称/同义词/聚合表达式/归属表/固定过滤，`/api/metrics` CRUD，每源最多 200 条）；问数阶段一按「问题命中指标名/同义词 → 模板化注入口径」生成 SQL，保证同一指标全系统口径一致。指标文本仅注入 prompt，生成结果仍过安全执行层。

**行级权限**（P1-3）：数据源 scope 支持登记 `rowFilters`（表 → SQL 谓词，如 `region LIKE '安徽省%'`）。谓词落盘前经清洗校验（拒多语句/注释/子查询/INTO/超长）；所有真实执行链路（问数、报表、SQL 重跑、下钻）执行前由 AST 强制把受控表包裹为过滤派生表，注入失败 fail-closed 拒绝查询。

### 3.5 知识库（含外部知识库接入）

为 NL2SQL 提供领域知识检索。维护业务术语、指标口径、字段含义等，问数时按相关性检索并注入提示词，带 token 预算控制。

**外部知识库接入（接口配置仅管理员）**：在企业本地知识库之外，可接入企业级外部 RAG / 知识服务作为智能问数自主学习的又一来源：

- **检索协议**：`POST {接口地址}`，请求体 `{ "query": "...", "topK": 4 }`；认证支持无 / Bearer Token（API Key 以 AES-256-GCM 加密落库，列表与详情不出明文）；响应兼容 `results` / `documents` / `data` / `items` 数组字段（每项取 `content` / `text` / `chunk` / `pageContent`），Dify、RAGFlow、自建网关均可适配。
- **生效范围**：全部数据源或指定数据源；支持启用开关、超时控制（500~30000ms）与连通性测试（表单内「测试连接」即时验证，不落库）。
- **问数注入**：与本地知识库并行检索，片段以「外部·源名」前缀注入阶段一 prompt；独立 token 预算（1200），不挤占本地知识注入槽位；单源失败降级为空不阻断问数。
- **入口**：知识库面板底部「外部知识源接入」卡片（仅 ADMIN 可见），提供接入/编辑/删除/测试；管理接口 `/api/knowledge-external` 全部仅 ADMIN。

### 3.6 SQL 样例库（Training Data）

管理「问题-SQL」训练样例，是 few-shot 注入与自学习的数据基础。

- **来源类型**：`MANUAL`（手工登记）、`FEEDBACK_UP`（点赞自动沉淀）、`IMPORT`（批量导入）。
- **登记/编辑/剔除**：支持对单条样例增删改。
- **批量导入冷启动**：粘贴多条 SQL 脚本，系统调用 LLM 逐条反推一句中文问题，可编辑预览后批量入库（问题留空则跳过）。
- 样例仅允许 `SELECT` 语句，长度受限（问题 ≤500 字、SQL ≤2000 字）。

### 3.7 数据自省（Data Introspection）

数据源级能力（默认关闭）。开启后，问数第一阶段模型可输出第三种契约：

```json
{ "needIntrospection": true, "intermediateSql": "SELECT DISTINCT ... LIMIT 30", "note": "..." }
```

系统真实执行该探查 SQL，把最多 30 行结果回喂模型，再生成最终 SQL，用于解决「取值不确定」导致的歧义。仅在首次尝试的首个候选上允许一轮，防止递归。

### 3.8 技能库（Skills）

为问数面板提供可复用的提示模板（prompt template）。

- **作用域**：`SYSTEM`（系统库，管理员维护）、`USER`（个人库，登录用户自建）。
- **分享流**：个人技能可发起分享（PENDING_SHARE），由管理员审核（approve/reject）后进入系统库。
- 技能支持占位符（placeholders），在问数面板中一键套用。

### 3.9 系统管理（管理员）

用户账号与角色权限配置（AdminPanel）：新增用户、调整角色、停用账号、重置密码等。

Token 用量查询（LlmUsagePanel）：管理员可随时按时间范围（近 7/14/30/90 天）查询每个用户的 LLM Token 消耗量，含输入/输出/总 Token、调用次数与成功率、平均耗时、最近使用时间及占比；同时提供按引擎/模型的调用汇总（多引擎成本对比）。数据来自 `/api/system/llm-usage`（仅 ADMIN 可访问），无用户上下文的历史记录归入「系统/后台」。

### 3.10 智能问数报告模式（v0.5.0 新增）

在智能问数输入区开启「报告模式」后，提问不再返回单条查询结果，而是直接生成一份完整分析报告（高管摘要 + KPI 卡片 + 图表组 + 洞察）。

- **模板选择**：开启报告模式后显示模板下拉框，可选择预设/自定义模板；默认「智能推断」——不选择模板时系统根据提问内容自行组织报告结构。
- **生成链路**：`POST /api/report/generate-from-query`，复用报表中心双阶段 live 链路（真实 SQL 执行），报告落库 `query_reports` 表；真实链路未命中时降级为演示数据且不入库（前端如实提示）。
- **对话内报告卡片**：生成成功后，对话流中展示报告摘要卡片（标题、摘要前 100 字、KPI/图表/洞察数量），点击「查看完整报告」跳转报告中心详情。
- **权限**：仅 ADMIN/ANALYST 可生成；与智能问数共享限流配额与并发互斥。

### 3.11 问数报告中心（v0.5.0 新增）

侧边栏「问数报告中心」入口，集中展示智能问数报告模式生成的报告。

- **报告列表**：卡片式展示（标题、模板名、提问摘要、KPI/图表/洞察数量、生成时间），按数据源隔离，普通用户仅见本人报告，ADMIN 可见全部。
- **报告详情**：内嵌复用 ExecutiveReportCard 完整展示，支持导出 PDF/PPT、异常扫描、协同批注。
- **删除**：仅本人或 ADMIN 可删除。
- **与报表中心的区分**：报表中心是手动选模板生成，报告中心是问数对话生成。

### 3.12 报告模板管理（v0.5.0 新增）

系统管理新增「报告模板」页签（仅 ADMIN）。

- **预设模板**：系统初始化内置 4 个（综合经营分析/资产质量与风险监控/投资收益与财务分析/企业战略决策简报），标记「系统预设」不可编辑/删除。
- **自定义模板**：管理员可新增/编辑/删除，表单化编辑章节结构（章节标题 + 分析提示词 + 图表类型），无需手写 JSON。
- **模板结构**：`{ sections: [{ title, prompt, chartType }] }`，服务端校验合法性（`validateTemplateContent`）。

---

## 4. 智能问数处理链路

一次提问在服务端的完整处理顺序：

1. **输入防护（L1）**：`queryGuard` 500 字截断 + 注入检测。
2. **鉴权与角色**：仅 `ADMIN`/`ANALYST` 可问数。
3. **限流**：`rateLimiter` + 每用户查询配额 + 并发槽位（进行中查询互斥）。
4. **Schema 上下文加载**：落库 schema → scope 白名单 → 敏感列过滤 → 5 分钟缓存。
5. **语义缓存**：命中则直接返回（`CACHE`）。
6. **生命周期钩子**：`emitBeforeQuery`。
7. **圈表**：关键词粗排 + embedding 精排（`selectRelevantTablesAsync`），embedding 不可用时降级纯关键词。
8. **上下文并行构建**：few-shot 样例（团队库 top-3 + 个人对话沉淀）、本地知识库 RAG、**外部知识库检索**、语义指标命中、点踩反例六者并发执行（各自失败降级不阻断）；知识类注入带 token 预算截断，推导过程 trace 中记录「外部知识库 N 字（成功 M 源）」。
9. **复杂度评估与清洗链**：LLM 判定 simple/multi-step；多步或「深度分析」开关开启时执行中间表清洗链（物化 `ait_*` 至应用库），失败不阻断。
10. **阶段一 SQL 生成**：可能触发歧义澄清或数据自省；携带已批准计划（planId）时按计划执行并跳过澄清。
11. **安全执行**：`executeSafeSql` 仅放行只读查询；SQL 仅引用已注册 `ait_*` 时改在应用库执行（白名单校验）。
12. **阶段二分析**：基于真实数据生成解读与图表配置。
13. **审计落账（L6）**：记录状态 `SUCCESS`/`FALLBACK`/`CLARIFY`/`CACHE`/`DENIED_*`。
14. **生命周期钩子**：`emitAfterQuery`。

全链路各步骤同步写入 `query_trace`（推导过程回放数据源）。

---

## 5. AI 引擎

- LLM 通道统一收敛在 `server/llmClient.ts`，支持三引擎：**Ollama**（本地）、**通义千问百炼**（OpenAI 兼容协议）、**Gemini API**。
- 引擎选择优先级：**用户自选（请求级，AsyncLocalStorage 传递）> `AI_ENGINE` 显式指定 > 密钥存在性自动 > Ollama**。
- 用户自选：问数输入框旁的模型下拉，目录由 `GET /api/system/models` 给出；非法引擎/模型名返回 400；选择写入请求体 `model: {engine, model}` 并纳入结果缓存键。
- 当前主引擎：`Qwen qwen3.8-max`（云端百炼，`AI_ENGINE=qwen`，端点由 `QWEN_URL` 配置）；阶段路由统一云端——简单单表 SQL 生成与数据解读分流 `qwen3.7-flash`（`LLM_SQL_*`/`LLM_ANALYSIS_*`），复杂/多表 SQL 强制主模型保口径（v0.8.1 分档机制）；embedding 用 `text-embedding-v4`；本地 Ollama 保留为备用（切回：`AI_ENGINE=ollama`，OLLAMA_URL/LLM_MODEL 配置保留）。注意：`deepseek-r1` 系列为推理模型，思考链分钟级，禁止用于任何阶段（会突破问数超时上限）。
- 通义千问：默认模型 `qwen3.8-max`（`QWEN_MODEL` 可改）；普通按量 Key 用默认端点，Coding Plan Key（`sk-sp-` 开头）必须配专属端点 `https://coding.dashscope.aliyuncs.com/v1`，且官方限定仅可在编程工具中使用。
- Embedding 用于圈表精排（Ollama 需本地 embedding 模型，千问用 `text-embedding-v4`），不可用时自动降级纯关键词粗排。
- 引擎信息通过 `GET /api/system/engine` 暴露给前端（不泄露内网地址），侧边栏底部有引擎运行状态指示灯。

---

## 6. 安全与合规

- **密钥管理**：生产环境（`NODE_ENV=production`）缺失 `JWT_SECRET` 时拒绝启动（fail-fast），防止伪造 token。
- **安全响应头**：`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`；生产启用 HSTS 与 CSP。
- **输入防护**：问题截断、注入检测、历史消毒。
- **只读执行**：样例与问数 SQL 强制 `SELECT` 白名单；字符串字面量在校验副本中置空防关键字干扰，但最终执行 SQL 保留原文字面量值。
- **行级权限（P1-3）**：scope.rowFilters 经 AST 强制注入为过滤派生表，覆盖子查询/UNION/JOIN，谓词非法或注入失败一律拒绝查询（fail-closed）。
- **敏感列过滤**：Schema 上下文加载时剔除敏感字段。
- **可观测性**：`requestLogger` 记录 requestId 与 API 访问日志（鉴权前，被拒请求同样留痕）；`auditLog` 记录问数审计。
- **默认回环**：服务默认绑定 `127.0.0.1`，需显式设置 `HOST=0.0.0.0` 才对外暴露。

---

## 7. 技术栈

| 层 | 技术 |
|----|------|
| 前端 | React 19 + Vite + TypeScript + Tailwind CSS 4 + Zustand + Recharts + motion |
| 后端 | Express 4 + Node.js（tsx 开发 / esbuild 打包） |
| 数据 | MySQL（mysql2）、PostgreSQL/Greenplum（pg） |
| AI | Ollama（本地）/ 通义千问百炼 / Gemini API，node-sql-parser 解析 SQL |
| 导出 | pptxgenjs（PPTX）、Python ReportLab（PDF，服务端原生排版） |
| 测试 | Vitest（54 个文件 / 569 用例） |

**启动命令**

```bash
npm install          # 安装依赖
npm run dev          # 开发：tsx server.ts（前端 Vite 内嵌）
npm run build        # 打包前端 + 服务端
npm start            # 运行打包产物
npm test             # 运行 Vitest
npm run lint         # TypeScript 类型检查
```

---

## 8. 帮助功能

- 系统 Header 右上角提供「帮助」按钮，打开帮助面板（HelpModal）。
- 帮助面板通过 `GET /api/help/manual` 实时读取 **`docs/用户使用指南.md`** 并渲染——指南面向终端使用者，按「三分钟上手 / 各模块怎么用 / 常见问题」组织，回答「系统怎么用」。
- 服务端按优先级回退：指南文件缺失时自动回退读取本说明书，保证帮助入口始终可用。
- 双文档分工：本说明书是**开发与评审侧**的功能单一事实源；《用户使用指南》是**终端用户侧**的操作向导。功能变更时两者同步维护：说明书记「是什么」，指南记「怎么用」。

---

## 9. 变更记录

| 日期 | 版本 | 变更内容 | 维护人 |
|------|------|----------|--------|
| 2026-08-29 | v0.9.9 | 全云端模型统一（阶段路由切换）：① 简单单表 SQL 生成与阶段二数据解读两路阶段路由由本地 Ollama qwen3:8b 统一切换为云端 qwen3.7-flash（LLM_SQL_ENGINE/LLM_ANALYSIS_ENGINE=ollama→qwen），全链路（主模型 qwen3.8-max / 快速模型 flash / embedding text-embedding-v4）不再依赖本地 Ollama，消除本地模型常驻内存占用（27b-mlx 23GB 释放）与 8b 模型换入换出的额外延迟；② v0.8.1 分档保护机制不变——复杂/多表 SQL 仍强制 qwen3.8-max 主模型（flash 多表 JOIN 有漏 COUNT(DISTINCT) 系统性偏差），本地 Ollama 配置保留可一键切回；③ 实测：重启后真实问数「拜访记录一共有多少条？」83s 成功（判复杂走 3 候选择优：sql_gen 58.6s=首候选 6.6s 串行+补发 2 候选并行 44.3/51.8s，阶段二 flash 解读 24.4s），llm_usage 近 2 小时全部 qwen 云端调用零 ollama 记录；④ 已知风险——云端 max 单次调用历史失败率约 19%（含一次 543.5s 超时失败致 FALLBACK），复杂问题建议关注云端配额与超时表现 | 系统 |
| 2026-08-29 | v0.9.8 | 收缩态侧边栏菜单悬停提示（UI 微调）：① 原收缩态菜单仅用浏览器原生 title 属性（出现慢约 1-2s、样式与深色主题不一致、只能单行文本），改为自定义悬停浮层——鼠标悬停图标即时在右侧弹出深色提示卡，含主标题（白色粗体）+ 徽章 chip（如「AI 交互」「N 源」）+ 功能副标题（灰色小字，说明该菜单用途），用户无需展开侧边栏即可知晓各菜单功能；② 纯 CSS 实现（group-hover:block），零 JS 状态零重渲染；浮层绝对定位（left-full top-1/2 ml-2 z-50）脱离文档流，pointer-events-none 不拦截鼠标事件，显示/隐藏不引发布局跳动；③ 同步覆盖收缩态「展开侧边栏」按钮与底部引擎状态点（引擎名 + 运行状态），三处原生 title 全部移除避免双提示；④ 实测：tsc 三档 + lint:strict 零错误，DOM 层验证浮层定位 x=62 不被侧边栏裁切、三要素齐全、默认 display:none 悬停触发 | 系统 |
| 2026-08-29 | v0.9.8 | 左侧菜单回行修复（UI 微调）：① 菜单项主标题加 truncate（原本只截断副标题，label 无换行约束，遇徽章挤压时文字回行或溢出无省略号）；② 徽章加 shrink-0 + whitespace-nowrap + ml-2（「AI 交互」「自动生成」等 4 字徽章原本被挤成两行）；③ 图标盒与 Schema 折叠标题加 shrink-0/truncate；④ 底部引擎状态卡片标题加 truncate（「Ollama qwen3.8:27b-mlx 引擎运行中」原本「中」字落第二行）；浏览器实测 7 个菜单项主/副标题、5 个徽章、Schema 标题、引擎卡片标题全部单行显示，超长一律省略号截断 | 系统 |
| 2026-08-29 | v0.9.8 | 知识库漂移检测（企业级改进 Phase 3-3）：① **检测引擎**——新增 `server/driftDetector.ts`：低基数枚举维度列（业务分类等）取值快照比对；观察名单 `kb_drift_watch`（数据源×表×列，values_json 为最近一次取值快照，NULL=未建基线）；名单为空时按 Schema 自动发现（字符串列且排除编号/名称/日期类 NON_ENUM_RE 模式，COUNT(DISTINCT) ≤ 50 判定低基数）；扫描逐列 SELECT DISTINCT 与快照比对——首次仅建基线不产生事件，有差异则写 `kb_drift_events`（OPEN）并更新快照，同列相同内容的 OPEN 事件不重复告警（`<=>` NULL 安全比较去重）；取值读取走安全执行层（SELECT-only/超时/chain 场景配额），仅读枚举元数据不读事实行；标识符（表/列名）拼入 SQL 前强制 IDENT_RE 校验；startDriftSweeper 每日定时全量扫描（unref 不阻塞进程退出，间隔可用 DRIFT_SCAN_INTERVAL_MS 覆盖）；② **管理 API**——新增 `server/routes/opsDrift.ts`（仅 ADMIN）：GET /api/ops/drift 事件列表（OPEN 优先按时间倒序）+观察列总数、POST /drift/scan 手动扫描（dataSourceId 可选，缺省扫全部数据库型数据源）、POST /drift/watch 登记观察列（幂等/标识符 400）、DELETE /drift/watch 移除（404）、POST /drift/:id/ack 确认（OPEN→ACKED，重复 404）；③ **前端面板**——新增 DriftAlertPanel（系统管理·质量页签顶部）：OPEN 事件琥珀色提醒卡片（表.列/新增取值 chips/消失取值 chips 删除线/检测时间/「知道了」确认按钮），无 OPEN 时灰绿安心条显示观察列数，「立即扫描」手动触发并回报汇总，已确认事件折叠置灰展示；④ **验收实证**——临时脚本挂真实路由+真实库：tmp_drift_probe 登记观察→首次扫描建基线 newEvents=0→人工 INSERT 新枚举值→再扫描 newEvents=1→事件列表 OPEN 且 added 含新值→重复扫描去重 newEvents=0→ack 200/重复 ack 404→非法表名 400→未认证 401→移除 200/重复移除 404，13 断言全过；⑤ 实测：tsc 三档零错误、driftDetector 纯函数单测 12 用例（diffValues 六场景/discoverEnumColumns 排除规则/IDENT_RE）、docs:check 111 端点（+5）、state:check 通过（状态全落库无新增进程内状态） | 系统 |
| 2026-08-29 | v0.9.7 | 准确率在线度量看板增强——四率周报与数据源下钻（企业级改进 Phase 3-1）：① **周趋势补齐点踩率**——WeeklyTrendPoint 增加 downRate 字段（= DOWN/(UP+DOWN)，无反馈周为 null 而非 0），此前周报有点赞率缺点踩率，四率（点踩率/自省触发率/拒答率/澄清率）按周呈现不完整；② **按数据源下钻**——GET /api/ops/metrics 新增可选 query 参数 dataSourceId（trim 后为空视为不过滤），audit 十态/feedback 反馈/trace 自纠链子查询/平均耗时四条 SQL 统一拼接 `AND data_source_id = ?`（参数化防注入），响应回显 dataSourceId 供前端核对；query_audit_log/query_feedback/query_trace 三表本就有 data_source_id 列与索引，过滤走索引无全表扫描；③ **前端看板改造**——OpsMetricsPanel 控制条新增数据源下拉（选项取自 useAnalyticsStore.dataSources，默认「全部数据源」汇总），切换即重新拉取（loadMetrics 依赖 dsFilter）；趋势图周模式由「成功率/缓存命中率」改为验收口径四率（点踩率 #fb7185 / 自省触发率 #818cf8 / 拒答率 #f472b6 / 澄清率 #fbbf24），日模式保持原四线（成功应答率/缓存命中率/澄清率/拒答率）；chartData 联合类型收窄为 `Record<string, string \| number \| null>` 修复 recharts LineChart 泛型推断冲突；④ **验收实证**——临时脚本挂真实路由 + 真实库：全部数据源近 30 天 totalQueries=541，按 ds_1786406744499（客户拜访管理）过滤后 218 ≤ 541，五个数据源各自过滤之和 508 ≤ 541（差额为无归属记录）、周趋势含 downRate、非法 dataSourceId 返回 200 且 totalQueries=0、未认证 401，11 断言全过；⑤ 实测：tsc 三档零错误、opsMetrics 单测补 downRate 断言（有反馈/无反馈 null/跨周聚合）66 文件 764 用例全过、docs:check 106 端点一致 | 系统 |
| 2026-08-29 | v0.9.6 | SSE 断线续传（企业级改进 Phase 2-5）：① **服务端重放缓冲**——新增 `server/sseReplayBuffer.ts`：问数流式事件按 traceId 入进程内缓冲（seq 从 1 单调递增作为 SSE `id` 字段；终态集合 done/clarify/refuse/error 到达后拒收；终态 TTL 10 分钟/进行中 30 分钟兜底/单 trace 500 条上限；60s 周期清扫 unref 不阻塞进程退出；listeners 订阅推送增量事件）；sseSend 改造为「先入缓冲再写流」，res.write 包 try/catch——客户端断开后 LLM 链路继续执行、事件仍入缓冲；② **新端点 GET /api/query/stream-replay/:traceId**——Last-Event-ID 请求头与 ?after= 查询参数双支持（fetch 重试无法自动带头），归属校验（仅本人或 ADMIN，traceId 格式正则拦截），先回放存量事件→未终态则 subscribeTrace 挂增量推送→seq 去重防回放/订阅竞态→终态到达或 5 分钟兜底收流；③ **续传锚点**——traceId 声明提前至 sseSend 之前（块作用域要求），stage 事件携带 traceId（首个阶段事件即可供前端记录续传锚点）；④ **前端自动续传**——sseStream 解析 `id:` 行并回调 onEventId 记录已收序号、error 事件挂 sseTerminal 标记区分「业务终态错误」与「网络中断」；QueryChat 问数请求重构为 attempt 重试循环（0=原始 POST / 1-2=GET 续传，间隔 1s），canResume 五条件判定（仅网络层中断且已拿到 traceId 才续传；用户主动停止/超时 AbortError、业务终态错误、重试上限、控制器已中止均不重试），续传期间进度提示「网络中断，正在自动续传…」；⑤ **部署指南**——DEPLOYMENT.md nginx 配置补 `proxy_buffering off`（SSE 事件实时下发）+ `proxy_read_timeout 600s`（容忍 LLM 推理长间隙）+ 多实例粘性会话说明（重放缓冲为进程内实现，重连落异实例 404 时前端降级完整重试）；state:check 白名单登记 local-accel 类（18 条）；⑥ **验收实证**——临时脚本挂载真实路由走 HTTP 全链路：断线后续传回放+订阅 seq [2,3,4] 连续不重不漏、终态收流、Last-Event-ID 头通道等价、终态后全量回放 0ms 即完、404/403/400 防护，9 项断言全过且全程无 SQL 执行（纯缓冲回放不重新跑查询链路）；⑦ 实测：tsc 三档零错误、docs:check 106 端点、state:check 18 条、新增 sseReplayBuffer 16 用例（序号递增/终态拒收/切片过滤/归属/订阅/退订/TTL 清扫/容量上限） | 系统 |
| 2026-08-29 | v0.9.5 | 数据源连接池分级与场景化执行超时（企业级改进 Phase 2-4）：① **场景三级分类**——`QueryScenario` 将 SQL 执行划分为交互问数（interactive：问数/下钻/指标预览/execute-sql，用户在线等待）、分析链（chain：报表生成/清洗链/评测与压测 golden 验证）、导出类异步任务（export：taskHandlers 报告生成 worker，经 runLiveReport scenario 穿透）；② **连接池分级**——数据源连接池由「按数据源 ID 单池」改为「按 数据源×场景 复合键分级建池」，配额独立：交互默认 dsPoolMax() 基准、分析链 max(2, ceil(基准/2))、导出 max(1, floor(基准/5))（DS_POOL_MAX=10 时恰为 10/5/2 计划基准），DS_POOL_INTERACTIVE/DS_POOL_CHAIN/DS_POOL_EXPORT 可显式覆盖（clamp [1,100]）；invalidateExecutorPool 按数据源前缀失效全部场景池，配置变更语义不变；③ **场景化执行超时**——QUERY_TIMEOUT_INTERACTIVE_MS=15s / QUERY_TIMEOUT_CHAIN_MS=120s / QUERY_TIMEOUT_EXPORT_MS=60s（原扁平 10s），MySQL 侧驱动 query timeout + 服务端 MAX_EXECUTION_TIME 优化器提示双保险（hint 在执行前注入、不经过 AST 校验器防误拦，5.7.8+ 生效老版本当注释忽略；返回调用方的 finalSql 保持业务 SQL 原貌不含 hint），PG/Greenplum 侧各场景池独立 statement_timeout；④ **验收实证**——DS_POOL_EXPORT=1 时双慢查询 SLEEP(3) 打满导出池（串行 3s/6s 证明占满排队、同池快查询 6s 证明配额真实生效），并发 3 个交互查询全部 32ms 秒回零排队；⑤ 实测：tsc 三档零错误、65/65 测试文件 748 用例全过（新增 sqlExecutorPool 14 用例：配额公式/超时档位/env 覆盖回退/hint 注入/池隔离/前缀失效） | 系统 |
| 2026-08-29 | v0.9.4 | 进程内状态巡检与 embedding 缓存版本号（企业级改进 Phase 2-3）：① **进程内状态巡检门禁**——新增 `scripts/check-stateless.ts`（`npm run state:check`）：基于 TypeScript AST 扫描 server/ + server.ts 的模块级空构造器 `new Map()`/`new Set()`（可变进程内存储），函数内局部变量与字面量初始化常量（`new Set([...])`）自动排除；白名单 STATE_WHITELIST 内嵌脚本（17 条首次全量盘点，各带存在理由与多实例分类：redis-fallback 单机内存回退 8 条 / local-accel 本地加速层 5 条 / registry 代码级注册表 1 条 / statestore-impl 内存实现本体 2 条 / cli-local CLI 主入口一次性状态 1 条）；双向校验——白名单外新增拦截、代码删除后白名单陈旧拦截；接入 CI quality 作业与 pre-push 门禁（5 步升 6 步）；② **schemaLinking embedding 缓存版本号**——表/列摘要向量缓存 key 由「digest 截断前缀」（200/120 字符，截断点后编辑不改 key 会陈旧命中，且超长公共前缀存在碰撞风险）改为「名称 + sha1(digest) 前 16 位内容指纹」（沿用 dataVersion 约定）：向量是 digest 文本的纯函数，内容寻址使编辑数据源 schema 后按表/列粒度自动失效（仅重算被编辑项，旧向量随 LRU 淘汰），内容不变稳定命中不过度失效；③ **多实例部署指南**补全本地加速层清单（embedding 缓存/数据版本指纹/Ollama 后端池/任务注册表）与巡检门禁使用说明；④ 实测：新增 13 用例（巡检脚本 9：真实仓库双向一致/fixture 正负向/函数内排除/字面量排除/类属性识别/陈旧拦截/对象属性识别/测试文件排除；缓存版本 4：命中零重请/按表粒度失效/截断碰撞回归/按列粒度失效），state:check 真实仓库 17/17 双向一致通过 | 系统 |
| 2026-08-29 | v0.9.3 | LLM 多后端路由与 embedding 批量化（企业级改进 Phase 2-2）：① **Ollama 多后端**——`OLLAMA_URLS=host1,host2`（逗号分隔、去重、优先于 OLLAMA_URL 单后端），后端池最少并发路由（取 inflight 最小健康节点），调用失败即摘除（15s 冷却）并自动切换次优健康节点重试，全部摘除时取最早恢复节点尽力服务不拒绝；后台健康检查每 10s 探测被摘除节点（GET /api/tags 3s 超时），恢复即重新接入（unref 定时器不阻塞进程退出）；chat/embedding/流式/模型目录四处调用点统一走后端池，引擎标签多节点时显示「@ N 节点」；② **embedding 批量化**——新增 callEmbeddingBatch（与单条版同截断/角色前缀/缓存规则，返回等长数组失败项为 null），Ollama 走 /api/embed input 数组（老版本 404/400 自动回退逐条 /api/embeddings，诚实降级不丢文本），Qwen OpenAI 兼容协议按 index 归位，Gemini 逐条并发；单批上限 EMBED_BATCH_SIZE（默认 16，封顶 64）；③ **调用点批量化改造**——知识库导入 saveKnowledgeDoc 由逐块循环改为整批（往返 N→ceil(N/16)）；schemaLinking 圈表/宽表列裁剪由 Promise.all 逐项调用改为跨表合并候选一次批量预填后同步读缓存打分；④ 实测：tsc 三档零错误、62/62 测试文件 721 用例全过（新增 llmBackends 11 用例：配置解析/最少并发/剔除/兜底/故障转移/健康恢复/批量/缓存/分批上限/老版本回退/批量走最闲节点）；端到端实证——双后端（首位为宕机节点）问数 102s 成功返回 live 结果、宕机节点摘除日志确认、问数不中断；知识库导入 3 块文档仅 1 次 embedding 网络往返（llm_usage 计数 2611→2612，降幅 66.7% ≥50% 目标），3 块向量全部落库 | 系统 |
| 2026-08-29 | v0.9.2 | 长任务队列化（企业级改进 Phase 2-1）：① **MySQL 任务队列**——新增 async_tasks 表 + 内置 worker（不引入外部 MQ）：MySQL 8 `FOR UPDATE SKIP LOCKED` 事务内原子领取，多 worker/多实例不重复消费；执行期 5s 心跳，RUNNING 且心跳超 90s 判定孤儿——attempts<2 回 PENDING 重跑、达上限标 FAILED「服务重启导致任务中断（已达最大重试次数）」，实例重启不静默丢任务；② **交互隔离**——worker 独立并发上限 TASK_WORKER_CONCURRENCY=2，异步任务不占用户交互问数并发槽（acquireQuerySlot），分钟级报表生成期间问数 P95 不劣化；每用户在途任务上限 TASK_QUEUE_USER_MAX=3，超限 429；③ **异步端点**——报表生成/报告模式生成/PDF 导出新增 `/generate/async`、`/generate-from-query/async`、`/export-pdf/async`（202 即返 taskId），新增 GET `/api/tasks/mine`、`/api/tasks/:id`（本人或 ADMIN）、`/api/tasks/:id/download`（文件结果经服务端落盘 data/task-results 下载，result 中剥离 file 标记附 downloadUrl）；④ **前端**——三调用点（报表中心生成/自动重生成、报告模式、PDF 导出）改提交后轮询（src/utils/asyncTask.ts pollTask/downloadTaskResult，Bearer 鉴权 + blob 下载）；⑤ **审计**——AuditStatus 新增 QUEUED 入队留痕，终态由 worker 侧落账；⑥ **集成验证修复两缺陷**——id 列宽 VARCHAR(40)→48（`task_<uuid>` 实为 41 字符，存量表幂等 ALTER 迁移）；Express 4 async handler 抛错不被自动接管导致请求挂起，三异步端点 submitTask 统一 try/catch 返回 500；⑦ 实测：tsc 三档零错误、61/61 测试文件 710 用例全过（新增 taskQueue 16 用例）、docs:check 105 端点一致；端到端闭环实证——提交 202 秒回→轮询 SUCCESS（PENDING→RUNNING→SUCCESS 状态机）→下载 PDF（%PDF- 魔数 3586 字节）→/mine 列表；孤儿恢复两路径实测通过（插入心跳过期 RUNNING 任务：达重试上限标 FAILED 留因、未达上限回队重跑并由 worker 置终态）；审计 QUEUED 落库确认 | 系统 |
| 2026-08-29 | v0.9.1 | 流程护栏与 E2E 防线（企业级改进 Phase 1）：① **pre-push 本地门禁**——husky 钩子推送前强制跑 tsc 三档 + OpenAPI 同步校验 + vitest 全量，任一步红灯即拒绝推送（正/负向验证均通过：注入类型错误被拦在第 1 步）；② **docs:check 纳入 CI** quality 作业，OpenAPI 漂移在 PR 阶段即拦截；③ **仓库清卫生**——scripts/ 下 17 份一次性交付报告归档 docs/archive/，评测/压测报告 JSON 取消 git 跟踪（.gitignore 既有规则生效，文件保留本地）；④ **E2E 冒烟新增 3 条 critical 回归防线**——登录后主界面渲染且 ErrorBoundary 白屏兜底不触发、核心 API 一律返回 JSON（健康检查 200/未知路由 404 JSON/未授权 401 JSON，对照实验证明 SPA fallback 仅对非 /api 路径返回 HTML）、知识库面板 CRUD 路由在线且渲染无错误横幅；⑤ **flexquery E2E 按 v0.4.15 现行 UI 重写**——点击加字段（替代旧拖放锚点）、JOIN 下拉化配置、行数选择器对齐 resetBuilder 选表后回退防爆量 100 的实际行为；选源策略改为 API 引导（自动选中含问数范围表的库表类数据源，MySQL 优先）；FlexQueryBuilder 数据源/表选择器增加 data-testid；⑥ **PG 方言双引号标识符误拦修复**（E2E 暴露的真实缺陷）——stripCommentsAndStrings 增加方言参数：pg 保留双引号段（PG/Greenplum 双引号是标识符），此前灵活查询/LLM 生成的加引号 PG SQL 全部被安全执行层误拦为「SQL 未引用任何数据表」；validateSelectSql/injectRowFilters 同步传入方言，新增 4 条 PG 方言回归单测；⑦ 实测：tsc 三档零错误、60/60 测试文件 694 用例全过、docs:check 99 端点一致、E2E 9/9 通过 | 系统 |
| 2026-08-29 | v0.9.0 | 质量门禁清零（企业级评估 P0 批次）：① **TDZ 自引用修复**——`server.ts`、`server/routes/help.ts`、`server/pdfExport.ts` 三处 `const __dirname = typeof __dirname ...` 自引用在 ESM/tsx 下命中暂时性死区，统一改用 `fileURLToPath(import.meta.url)`（esbuild 打包 CJS 时自动降级为 __filename 的 file URL，双环境正确），修复后 pdfExport.test.ts 恢复加载；② **React 类型补齐**——安装 `@types/react`/`@types/react-dom`（此前 react 全程以 any 编译，ErrorBoundary 类组件报错即为表征），并修复补齐类型后暴露的 DynamicChart `percent` 可能 undefined 问题；③ **类型漂移修复**——`ColumnSchema` 增加 `isPartition`/`isFilter`（快照分区键/强制过滤列元数据）、`DataSource.config` 增加 `password`（仅服务端使用），seedDataResources 种子数据与类型对齐；knowledgeServices 泛型改回 codebase 主流 `(rows as any[])` 模式；`extractDbSchema` 类型收窄简化；`LlmChannel` 补 `text_stream`；`StreamingChunk.type` 补 `error`；Gemini 流式调用更正为 `generateContentStream`（@google/genai v2.4 已移除 streamGenerateContent）；④ **OpenAPI 全量同步**——补齐 39 个缺失端点（OIDC/ACL/data-version/flex-schema/访问申请/导出审批/外部知识库/知识库导入导出/指标审批与版本/PDF 导出/问数报告/报告模板），docs:check 恢复 99 端点一致；⑤ **基线 tsconfig 未启 strictNullChecks 的联合类型窄化陷阱修复**——FlexQueryBuilder 三处布尔判别式改 `=== false/true` 显式比较；evalRunner 测试字面量控制在 15 位有效数字内消除 eslint no-loss-of-precision；⑥ 实测：tsc 三档（lint/lint:server/lint:strict）零错误、eslint 零 error、60/60 测试文件 690 用例全部通过、docs:check 通过 | 系统 |
| 2026-08-27 | v0.8.8 | P0-2 统一 SQL 自纠错环基建：① **问题**——清洗链步骤自愈（v0.8.3）、计划模式自愈（v0.8.2）各自独立实现，缺乏统一出口审计；阶段一 SQL 白名单校验失败时无自愈能力；② **架构改造**——在 `server/sqlExecutor.ts`核心安全执行层增加通用自愈入口接口：新增 `RetryStrategy`类型定义与 `buildRetryPrompt` 纠偏 Prompt 生成器，供调用方在 SQL 校验/执行失败时传入 LLM 自愈回调；保持 `validateSelectSql`同步函数签名不变以兼容现有全部调用链（690 测试用例无改动）；③ **设计原则**——自愈逻辑不侵入基础校验层，由上层编排（清洗链/计划生成/未来可能的阶段一自愈）按需注入；统一日志出口便于追踪自愈成功率与失败归因；④ 全量 690 单测通过、tsc 零错误 | 系统 |
| 2026-08-23 | v0.8.5 | D5 失败用例归因修复（宽表评测 wt01/wt02/wt09）：① **数据源名注入 prompt**——SchemaContext 全链路增加 dataSourceName 字段（schemaContext.ts CacheEntry/SchemaContext/fromClientSchema/loadSchemaContext 四处），routes/query.ts 传递到 runLiveQuery，liveQuery.ts LiveQueryInput 接口与 buildStage1System 签名同步增加参数；阶段一 prompt 开头注入「当前数据源名为 XXX，这是系统名称不是数据值，严禁用作 WHERE 过滤条件；用户提及该名称时直接生成 SQL 不要触发澄清」，根治 wt01 库名「数据资源库」被误判为机构过滤值（JGMC='数据资源库'）；② **三条强制约束**——金额原值保护（除非用户明确要求换算单位，不要对金额列做除法换算）、SELECT 列纯净性（不要添加常量字符串作为标签列如 '项目总数' AS category）、模糊问题澄清（问题过于笼统没有指定具体指标或维度时应触发澄清而非直接返回概览），分别修复 wt01 金额被换算为亿元、wt02 生成 SQL 多带常量标签列、wt09 模糊问题未触发澄清；③ 690 单测全部通过、tsc 零错误；④ 评测结果（8b 模型基线）：准确率由 70% 提升至 80%（8/10），wt02/wt09 修复通过，wt01 因 8b 模型非确定性在 pass/error/fail 间波动（27b 主模型单用例验证通过） | 系统 |
| 2026-08-23 | v0.8.4 | 宽表评测基建与首轮基线：① **评测器三处修复**——`npm run eval` 新增 `--file` 支持按文件指定评测集（数据资源库宽表评测集独立存放，客户拜访库 104 用例默认集不动）；结果集比较由 6 位小数字符串等值改为数值容差比较（容差 max(0.005, 相对 1e-9)），吸收系统常用 `ROUND(x,2)` 金额写法与全精度 golden 的假阴性，口径错误（差异以千万计）不受影响；评测报告 generatedSql 捕获补齐 `result.generatedSQL` 字段（此前报告中生成 SQL 全为空）；② **数据资源库宽表评测集** `server/eval/evalCases.jichuang.json` 10 用例（8 条 result 覆盖四红线 + 1 clarify + 1 refuse），golden SQL 全部经进程内安全执行层预验证返回真实数据；③ **首轮基线 accuracy 70%**（7/10：single_agg 4/6、subquery 1/1、time 1/1、refuse 1/1、clarify 0/1），失败归因——wt01 库名「数据资源库」被误判为机构过滤值（JGMC='数据资源库' → SUM=NULL）且金额被换算为亿元、wt02 生成 SQL 多带常量标签列、wt09 模糊问题未触发澄清直接返回概览，列入质量手册 D5 修复清单；④ 新增容差单测 1 组（eval 单测 11 过、tsc 零错误） | 系统 |
| 2026-08-23 | v0.8.3 | 清洗链步骤自愈：① **问题**——深度分析/多步清洗链中，LLM 生成的清洗步骤 SQL 偶发编造列名/表名（真实事故：tbl_report_submission_data 上编造 template_id、report_assignments 主键 id 误用为 assignment_id），步骤执行直接报 Unknown column 失败；② **修复**——runAnalysisChain 步骤执行失败时自动携带失败 SQL + 执行错误 + Schema 契约让模型纠错重试一次（输出非法或与原 SQL 相同均不重试），重试仍失败合并两次错误留痕、不阻断后续步骤，自愈成功步骤在 trace 标注「纠错自愈」；③ 新增 3 单测（纠错自愈/重试仍失败留痕/非法输出不重试），全量 689 用例通过、tsc 零错误；实测固收台账深度分析全链路：纠错机制真实触发、合并错误正确留痕、链路继续正常完成 | 系统 |
| 2026-08-23 | v0.8.2 | 计划模式生成自愈：① **问题**——「先制定计划」模式下 LLM 偶发输出未通过结构校验（非纯 JSON / 缺 steps / 步骤缺 title），计划生成直接报「分析计划生成失败」，点击推荐追问（计划模式开启时同样走计划端点）一并受影响；② **修复**——generateQueryPlan 首次校验失败时自动携带纠偏指令重试一次（纯文字描述契约、不给可照抄的字面示例，防小模型拷贝示例充当输出），首次与重试失败均记录原始输出片段（截断 300 字）便于诊断；③ 新增 2 单测（纠偏重试自愈 + 重试仍非法抛错），全量 686 用例通过、tsc 零错误；实测两数据源多题计划生成 4-69s 全部成功 | 系统 |
| 2026-08-23 | v0.8.1 | 问数超时根治与链路提速：① **根因**——阶段二解读路由在本地 Ollama deepseek-r1:8b（推理模型思考链长、分钟级），叠加前端 200s 超时先于后端兜底到达，表现为「本地模型推理时间过长」；② **阶段路由最终落地为全本地双模型分工**——AI_ENGINE=ollama，主模型 qwen3.8:27b-mlx 承担复杂/多表 SQL 生成；简单单表 SQL 生成与数据解读分流 qwen3:8b（LLM_SQL_*/LLM_ANALYSIS_*，实测解读 43.6s→10.6s）；deepseek-r1 系列推理模型禁止用于任何阶段（思考链分钟级必超时）；注意双模型常驻需内存 ≥64GB（27b-mlx 23GB + 8b 11GB），内存不足时移除 8b 路由回退单模型；云端快速模型路由机制保留为可选提速项；③ **阶段一 SQL 生成按问题复杂度分档路由**（代码机制）——配置快速模型时简单单表走之，复杂/多表强制主模型保口径（实测 flash 多表 JOIN 漏 COUNT(DISTINCT) 致客户数 259 虚增为 278，同源多候选表决无法纠正系统性偏差），4 处生成调用点统一改为分档路由；④ **前端问数超时 200s→300s**容纳后端最坏预算（阶段一重试+多候选+执行+解读），杜绝后端兜底结果未返回即被前端中止；⑤ 超时文案中性化（不再误写「本地模型」）；⑥ 修复 metrics.test.ts 两处类型错误（createMetric 缺 as any、判别联合否定分支不收窄）；⑦ 文档同步——README/.env.example 主模型推荐改 qwen3.8:27b-mlx 并标注推理模型禁用原因。实测：全本地多表关联 96s（COUNT(DISTINCT) 口径正确、259 户一致）、启用 8b 分流后同题 47s（27b 生成 36.5s + 8b 解读 10.6s，口径不变）、云端参考 66s、多宽表 3 候选 160s 成功，均低于超时上限；三 tsc + 全量 685 单测通过、eslint 0 error | 系统 |
| 2026-08-21 | v0.5.3 | 报告 PDF 导出改用 ReportLab 服务端原生排版（根治截图方案错位/遮挡/复刻不全）：① **问题根治**——原前端 html2canvas 整页截图方案存在固有缺陷（oklch/oklab 颜色不兼容、对话框遮挡、长报告错位、无法完整复刻网页），改为 ReportLab 矢量文本排版（非截图），内容按文档流式布局天然无错位无遮挡；② **架构**——新增 POST /api/report/export-pdf 端点，Node spawn python3 子进程调 ReportLab，报告数据经 stdin JSON 管道传递（不进命令行参数无注入面）、stdout 收 PDF 二进制，超时 60s 强制 SIGKILL 防子进程挂起；③ **Python 脚本**（server/pdfgen/report_pdf.py）——A4 竖版/横版可选，中文用内置 CID 字体 STSong-Light（无外部字体依赖、跨平台稳定），封面标题+报告日期+模板、高管摘要、核心指标表格、战略洞察与建议（利好/预警/风险/洞察彩色标签）、图表分析（前端 ECharts base64 PNG 嵌入，无图时纯文字解读兼底），页眉含系统名+标题、页脚含页码；④ **安全**——L2 角色复核（仅 ADMIN/ANALYST）、normalizeExportData 归一化截断复用、脚本路径白名单候选（非用户可控）、PDF 头 %PDF- 校验；⑤ **前端**——ExecutiveReportCard 导出改为收集 ECharts getDataURL() 图表 + 调新端点下载，删除 html2canvas oklch 转换死代码（约 180 行）、pdfTheme 状态与主题选择 UI、移除 html2pdf.js 依赖；⑥ **测试**——新增 pdfExport.test.ts 7 用例（环境探测/路径解析/竖版/横版/最小报告/非法 base64 容错），全量 54 文件 569 通过；实测导出 PDF 中文排版无乱码、竖版 A4（595×842）/横版（842×595）正确、无权限 401 拒绝、内容完整无错位遮挡 | 系统 |
| 2026-08-20 | v0.5.2 | 报告金额单位与问数口径对齐：修复报告（报表中心/报告模式/计划模式）不按问数页选定金额单位换算的问题——① **链路贯通**：/api/report/generate、/plan、/generate-from-query 三端点接收 amountUnit，白名单校验（非法值 400 拒绝并审计 DENIED_INPUT），透传 runLiveReport/generateReportPlans；② **SQL 换算约定注入**：复用问数链路 buildAmountUnitPrompt（v0.4.5 确立的除以 1e8/1e6/1e4/1 + ROUND 两位小数 + 别名后缀约定），拼在报表阶段一用户消息首位；③ **口径互串防护**：报告计划存取记录生成时金额单位，批准执行时单位不一致返回 409「金额单位与计划制定时不一致，请重新制定」（Redis 与内存双通道均校验）；④ **前端**：报告模式提问自动携带当前选定单位；报表中心三处调用（手动生成/计划/自动重生成）读取 localStorage 共享键 app-amount-unit 沿用问数口径；⑤ 新增 4 个单测（单位约定注入 3 + 口径一致性 1），全量 562 通过 | 系统 |
| 2026-08-20 | v0.5.1 | 报表文案中文化：修复报告标题/摘要/洞察/KPI/图表解读中出现英文表名列名（如【dn_tzsy】）的问题——① **阶段一/阶段二 prompt 双重约束**：明确禁止文案中出现英文标识符，阶段二 prompt 注入完整「英文标识符 = 中文名」对照表（表名→displayName、列名→description）；② **服务端兜底替换**：新增 buildIdentifierNameMap / replaceIdentifiersWithChinese / sanitizeReportNarrative 纯函数，报告组装后对全文案字段做标识符边界替换（长词优先、不误伤包含关系，【dn_tzsy】连同书名号一起替换为中文名），LLM 不守约束时也能保证输出全中文；③ 新增 9 个单测（映射构建 2 + 替换 5 + 全字段清洗 2），全量 558 通过 | 系统 |
| 2026-08-20 | v0.5.0 | 智能问数报告模式：① **报告模式**——智能问数输入区新增「报告模式」开关，开启后提问直接生成完整分析报告（复用报表中心双阶段 live 链路），支持选择报告模板或默认智能推断；② **问数报告中心**——新增独立模块（侧边栏入口），卡片式列表展示问数生成的报告，详情复用 ExecutiveReportCard（PDF/PPT 导出、异常扫描、协同批注），仅本人或 ADMIN 可查看/删除；③ **报告模板管理**——系统管理新增「报告模板」页签，内置 4 个预设模板（不可编辑/删除），管理员可表单化新增/编辑/删除自定义模板（章节标题+提示词+图表类型）；④ **数据表**——新增 report_templates 与 query_reports 表；⑤ **测试**——新增 13 个单测（模板校验 10 + 报告转换 3），全量 549 通过 | 系统 |
| 2026-08-20 | v0.4.17 | 报表 PDF 导出对话框遮挡修复：导出前先关闭配置对话框（setShowPdfExportModal(false) + 等待 300ms 动画完成），避免对话框被 html2canvas 截取进 PDF 正文遮挡报告内容；浏览器实测验证 PDF 导出内容完整无遮挡 | 系统 |
| 2026-08-20 | v0.4.16 | 报表 PDF 导出修复：① **oklch/oklab 颜色兼容**——Tailwind v4 使用 oklch/oklab 颜色函数（含透明度修饰类），html2canvas 不支持解析，导致 PDF 导出失败；在 html2canvas onclone 回调中实现数学转换（OKLCH→OKLab→Linear RGB→sRGB），遍历克隆文档所有元素与 CSS 属性（color/background/border/fill/stroke/box-shadow 等），将 oklch()/oklab() 替换为 rgb()/rgba()；② **遮罩残留清理**——PDF 导出失败后 html2pdf 创建的全屏透明遮罩层（.html2pdf__overlay/.html2pdf__container）残留页面，拦截后续所有点击；在 catch/finally 中强制移除残留元素，避免锁死页面交互；浏览器实测验证：PDF 成功导出（1.8MB）、控制台零错误、页面交互正常 | 系统 |
| 2026-08-20 | v0.4.15 | 灵活查询跨表查询优化：① **字段选择跨表支持**——配置关联表后，字段面板自动合并主表 + 关联表所有字段（按维度/指标分组显示），关联表字段显示表名 badge 前缀（如 `[dim_region]region_name`），字段引用格式 `table.column`；② **JOIN 条件下拉化**——主表字段与关联表字段由文本输入改为下拉选择（关联表字段下拉根据选中的关联表动态加载 columns，未选表时禁用）；③ **字段添加逻辑跨表扩展**——addField 函数支持从合并字段列表（allFields）中查找字段（fullName 格式），拖拽/点击添加跨表字段到维度/指标区；④ **构建器跨表字段校验**——ident() 函数已支持 table.column 格式（v0.4.14），新增测试用例验证跨表字段作为维度/指标的 SQL 生成正确性（LEFT JOIN + GROUP BY 跨表字段）；536 单测全部通过 | 系统 |
| 2026-08-19 | v0.4.14 | P0-P3 综合优化：① **P0 取消 LIMIT 截断**——灵活查询行数上限由 100 提升至 10000（可选至 50000），服务端 MAX_ROWS 由 500 放宽至 100000，按真实记录数输出；② **P1 AST_FALLBACK 审计**——checkAstSafety AST 解析失败放行时标记 astFallback，execute-sql 路由检测并写入审计日志（status=FALLBACK）；③ **P1 版本号统一**——package.json/Header/README 统计信息统一为 v0.4.14；④ **P1 删除 bun.lock**——保留 package-lock.json 作为唯一锁文件；⑤ **P2-6 多表 JOIN**——灵活查询支持多表关联（INNER/LEFT JOIN），字段引用支持 table.column 跨表格式，UI 提供关联表配置区，测试覆盖 4 用例；⑥ **P2-7 E2E 固化**——新增 tests/e2e/flexquery.spec.ts，覆盖基础流程/多表 JOIN/行数上限 3 用例；⑦ **P2-9 慢查询治理**——执行按钮下方提示超时上限 10s，服务端 execute-sql 路由检测执行时长 > 3s 或行数 > 10 万时写入慢查询审计（detail 前缀 SLOW）；⑧ **P3-10 bundle 懒加载**——FlexQueryBuilder/CustomDashboard 改为 React.lazy 动态导入，减少首屏体积；⑨ **P3-11 a11y 键盘导航**——执行按钮支持 Enter/Space 键触发；⑩ **P3-12 差评回流评测集**——evalCases.json 新增 3 个差评用例（模糊问题/不存在表/敏感字段）；535 单测全部通过 | 系统 |
| 2026-08-19 | v0.4.13 | 灵活查询两项改进：① 页面布局改为上/中/下纵向堆叠（原三栏并排）——上：数据表与字段（选表下拉与搜索框并排，维度/指标分组左右并排各自独立滚动限高 36vh）；中：查询配置区（全宽）；下：查询结果，固定报表与查询历史两卡片全宽下左右并排；全屏显示（v0.4.12）与字段页签/搜索/点击添加（v0.4.11）能力保留；② 修复复杂 SQL 执行被拒「SQL 长度超出限制」——execute-sql/SQL 解读/下钻三个路由端点与安全执行层（sqlExecutor）的语句长度上限由 4000 统一放宽至 10000 字符，满足灵活查询多指标/多筛选/HAVING 组合生成的较长 SQL；SELECT-only + 表白名单 + 敏感列等安全校验不变；实测 5187 字符复杂 SQL 执行成功（29 行/514ms）；531 用例全部通过 | 系统 |
| 2026-08-19 | v0.4.12 | 灵活查询新增全屏显示功能：「查询配置区」与「查询结果」面板头部各新增全屏切换按钮，点击后面板以固定覆盖层（fixed inset-0 z-50）全屏展示，内部独立滚动且锁定背景页面滚动；支持 Esc 键或再次点击「退出全屏」返回三栏布局；查询结果全屏时图表高度自动放大（260→440px）、明细表分页容量增大（8→14 行/页），占比/透视/CSV/固化等操作在全屏下均可正常使用；纯前端交互改动无服务端变更，531 用例全部通过，浏览器端到端实测两处全屏进出/Esc 退出/全屏内执行查询与透视切换均正常 | 系统 |
| 2026-08-19 | v0.4.11 | 灵活查询页面布局优化（针对字段较多时操作不便）：① 左栏字段面板重构——多行平铺 chips 改为单行紧凑行式列表（独立内滚动容器限高 52vh，页面不被撑长），新增「全部/维度/指标」类型页签（带计数，字段多时只看一类减少滚动），维度/指标分组可折叠，搜索过滤后计数实时更新；字段行支持点击即添加（维度字段→分组维度、指标字段→聚合指标）与拖拽两种方式，已加入查询配置的字段显示对勾标记与高亮，避免重复查找；② 中栏配置区紧凑化——分组维度与聚合指标区改为左右并排（窄屏自动回落单列），拖放区最小高度 64→52px；筛选（WHERE）与指标过滤（HAVING）合并为可折叠「高级筛选」分组（标题带条件计数）；SQL 预览改为可折叠，收起时单行摘要（配置有误时红色高亮）；③ 顶部 Banner 紧凑化（三行标题区压缩为单行标题 + 一句话说明）；纯前端布局改动无服务端变更，531 用例全部通过；浏览器端到端实测：94 字段表页签/搜索/点击添加/对勾标记/并排布局/高级筛选折叠/SQL 折叠/执行 29 行 335ms/占比/透视全部正常，控制台无报错 | 系统 |
| 2026-08-19 | v0.4.10 | 灵活查询参照 Agile Query 功能增强：① 查询表达力——新增去重计数聚合（COUNT_DISTINCT，生成 COUNT(DISTINCT col)，别名 countd_前缀）；筛选操作符新增 BETWEEN 区间（中英文逗号分隔两端点，数量错误拒绝）与 IS NULL / IS NOT NULL（无值操作符不要求填值）；新增「指标过滤 (HAVING)」拖放区，聚合后条件过滤采用重复聚合表达式写法（非别名引用）保证 MySQL/PG 双方言安全；排序目标从固定首指标改为可选任一指标别名或维度列 + 升降序；② 分析体验——字段列表新增搜索框（按名称/描述过滤，参照搜索式字段定位）；结果区新增占比快速计算（首指标占总和百分比客户端追加列）、透视图（两维度行列交叉 + 单指标值，客户端透视不额外查库，需 2 维度且恰好 1 指标）、CSV 导出（BOM 头兼容 Excel 中文）；新增「最近查询历史」面板（执行成功自动记录，按配置去重上限 8 条，一键还原配置）；③ 兼容与修复——保存配置结构升级（orderByFirstMeasure → orderBy，载入旧配置自动迁移）；修复聚合方式变更后旧排序别名残留导致校验报错阻塞执行、排序下拉选「不排序」不生效的受控组件问题（维度/指标变化时自动校正失效排序目标）；flexQueryBuilder 测试扩展至 20 用例（新增 HAVING/BETWEEN/空值/去重计数/自由排序/复合组合），全量 531 用例通过；浏览器端到端实测字段搜索/去重计数/BETWEEN/HAVING/自由排序/占比/透视/CSV/历史还原/固化回归全部通过（HAVING 实测 SQL：GROUP BY 后 HAVING SUM(\`BNTFJE\`) > 1000000000 真实执行生效；透视表机构×报告期 20 列交叉渲染正常） | 系统 |
| 2026-08-19 | v0.4.9 | ① 新增数据自动更新增强：MySQL 8.0+ 默认缓存 information_schema 统计 24 小时（information_schema_stats_expiry=86400），INSERT 新增数据后 TABLE_ROWS/UPDATE_TIME 不变导致 v0.4.8 数据版本指纹漏检；数据版本探测改用专用连接 SET SESSION information_schema_stats_expiry=0 读实时统计（MySQL 5.7/MariaDB 无此变量时静默降级为原行为），PG 侧补充取最近 vacuum/analyze 时间最大值纳入指纹，实测 INSERT 后指纹立即变化；② 新增「灵活查询」功能模块（侧边栏入口，仅 ADMIN/ANALYST 可见）：拖拉拽可视化定制固定报表——选表后字段列表按维度/指标分组，拖入（或点击）分组维度、聚合指标（SUM/COUNT/AVG/MAX/MIN）、筛选条件（=、!=、>、>=、<、<=、LIKE、IN，IN 支持中英文逗号分隔多值），实时生成单表聚合 SQL 预览并真实执行；SQL 构建双道防线——客户端纯函数白名单校验（标识符仅允许字母数字下划线且必须存在于该表 Schema，统一引号包裹，筛选值单引号加倍转义，行数上限收敛至 [1,500]），服务端仍受 SELECT-only + 表白名单 + 敏感列 + 行级过滤约束；新增 GET /api/datasources/:id/flex-schema 端点向 ADMIN/ANALYST 单独开放只读 Schema（原 GET /api/datasources 对非 ADMIN 不下发 tables）；查询结果支持柱状/折线/面积/饼图/表格五种展示，可固化至决策数据看板（携带 sourceSql 自动参与 v0.4.8 数据变化自主更新）或保存为固定报表（localStorage 持久化，支持载入还原配置/删除）；新增 flexQueryBuilder.test.ts 11 用例与 dataVersion INSERT 场景用例，全量 522 用例通过；浏览器端到端实测拖拽构建→执行（29 行/209ms）→固化看板→保存固定报表→载入还原全链路通过 | 系统 |
| 2026-08-18 | v0.4.7 | 提问交互增强——拒答契约：用户提出与当前数据源无关（闲聊/常识问答/通用请求）或超出系统分析能力的问题时，不再用演示数据托底作答，而是如实反馈。阶段一 SQL 契约从三种输出扩展为四种（新增 `{"refuse": true, "reason": "..."}`，仅当 Schema 中不存在任何语义相近的表/字段时触发，存在可映射字段则仍尽力生成 SQL 并说明假设）；演示模式同步拒答（禁止编造演示数据）；路由层拒答不走 FALLBACK 分支，审计状态新增 REFUSED、对话历史同步落库（不参与 few-shot 检索）；前端拒答消息展示理由与拒答提示条（无图表/无数据）；新增 refusal.test.ts 7 个用例，全量 492 用例通过；同日补修：前端 SSE 解析器（sseStream.ts）终端事件白名单遗漏 refuse，流式模式下拒答消息不展示——白名单扩展为 done/clarify/refuse，sseStream.test.ts 补充回归断言，实测「今天天气怎么样」流式拒答返回具体理由；同日二次补修：拒答 prompt 的 reason 示例为固定模板句，小模型照抄导致用户只看到通用句无实质内容——live/sim 双链路 prompt 改为要求模型点明问题类型并说明数据源实际覆盖范围，服务端新增 enrichRefusalReason 兜底增强（理由通用/过短时自动拼接数据源实际覆盖的表清单），refusal.test.ts 新增 5 个用例，全量 497 用例通过，浏览器端到端复验「帮我写一首关于秋天的诗」拒答理由具体（点明文学创作类请求与财务业务数据范围）；同日话术统一：拒答复文采用固定模板「抱歉，我是数据分析助手，仅协助处理数据分析相关工作，无法处理XXXX」（XXXX 为请求类型简述如天气查询/写诗创作/编写Python代码），live/sim 双链路 prompt 同步，enrichRefusalReason 兜底改写未遵模板的输出（旧模板句/XXXX 占位未填/过短理由改写为统一话术并附数据源覆盖表清单），前端空理由兜底文案同步；全量 499 用例通过，r1:8b 实测三类无关问题均严格符合模板且正常问数不受影响（另：dev 脚本为 tsx 非 watch 模式，服务端代码改动需重启服务生效） | 系统 |
| 2026-08-18 | v0.4.8 | 决策数据看板与可视化决策报表自动检测数据变化并自主更新：服务端新增 dataVersion.ts 数据版本指纹（information_schema/pg_stat 行数与更新时间汇总 sha1 指纹，不做全表扫描，内存缓存 10s 防多端轮询风暴），新增 GET /api/datasources/:id/data-version 轻量探测端点（所有登录用户可访、不写审计避免轮询噪音，数据源不存在 404、非数据库型数据源返回 UNSUPPORTED 前端跳过）；前端新增 DataVersionWatcher 基线状态机（首轮探测仅建基线不触发、探测失败静默跳过不污染基线）与 useDataVersion 60s 轮询 hook；看板固化图表时携带 live 链路原聚合 SQL（sourceSql），检测到数据变化自动调 /api/query/execute-sql 重放刷新并展示监测状态行与「自动更新」角标，演示数据图表不参与自动更新；报表生成时记录自定义要求与数据来源（customPrompt/dataProvenance），检测到数据变化按原参数自动重新生成并就地替换（已有批注保留，与手动生成/待批准计划互斥，仅 live 报表监测）；新增 dataVersion.test.ts 与 dataVersionWatcher.test.ts 共 11 个用例，全量 510 用例通过；浏览器端到端实测：数据库变化后看板约 1 个轮询周期内自动重放刷新图表并打标，决策报表自动重新生成（紫色脉动→绿色完成徽章）且内容重渲染正常 | 系统 |
| 2026-08-18 | v0.4.6 | 阶段二解读失败不再输出无信息量兜底文案：原逻辑 LLM 异常时 `analysis={}` 静默吞错，前端仅剩「解读生成失败，使用兜底文案」空壳；新增 `buildFallbackAnalysis` 规则化降级解读（不依赖 LLM）——基于真实列统计与行数生成 aiExplanation（记录数/数值列总计均值区间/维度分组数/图表轴）、keyInsights（引用真实极值与均值，≤3 条）、kpiMetrics（前 2 数值列的总计/峰值，≤4 张），LLM 调用异常或返回空 aiExplanation 时自动接管，推导过程 trace 标题标注「【LLM 失败，规则降级】」便于事后追溯；新增 4 个单测（数值+维度列/纯维度/空 rows/多数值列封顶），全量 485 用例通过 | 系统 |
| 2026-08-18 | v0.4.5 | 问数金额单位自选 + 小数默认两位：① 问数页工具栏新增金额单位选择器（亿元/百万元/万元/元，默认亿元，localStorage 持久化随提问生效），所选单位注入阶段一 SQL 生成约定（金额列聚合结果除以对应除数 1e8/1e6/1e4/1，别名带 _yi/_baiwan/_wan/_yuan 后缀，列名/图表/解读沿用同单位）；后端白名单校验（非法值 400 拒绝并审计 DENIED_INPUT），单位进结果缓存键防口径互串；② 小数默认保留两位：阶段一强制约束新增数值精度规则（金额/比率/均值类 ROUND(2)，除法/换算必须包裹 ROUND，计数类保持整数），单位约定同步明确两位小数；③ 展示层统一千分位 + 非整数补足两位（数据表/KPI 卡片/图表 tooltip 与坐标轴，整数不补零），并移除图表 tooltip 原自动万/亿缩写（避免与所选单位二次换算冲突）；新增 3 个单测，全量 476 用例通过；实测万元口径 SQL 为 ROUND(SUM(BNTFJE)/10000,2) AS total_bntfje_wan（北京 720.26 亿 = 7,202,635.53 万精确一致），元口径带 total_bntfje_yuan 后缀 | 系统 |
| 2026-08-18 | v0.4.4 | 同比问数执行被拒修复 + 双模型分工提速：① 「同比增减变化排名」类两期对比 SQL（子查询聚合 + JOIN）被执行安全校验误拒（「引用问数范围外的表：sum/bntfje」）而降级演示数据——根因为表名白名单第一道正则防线的 FROM 段补充扫描按逗号分表时未排除子查询 SELECT 列表的逗号，把聚合函数名/列名误当表名；修正：段内含括号时跳过补充扫描（子查询内部真实表已由 FROM/JOIN 主扫描覆盖），新增回归单测 3 断言；② 引擎配置：主引擎换 Ollama qwen3.8:27b（生成 63s 一次通过，较 deepseek-r1:32b 提速一倍），阶段二数据解读路由 deepseek-r1:8b（LLM_ANALYSIS_ENGINE=ollama，llm_usage 实证 74s）；③ 同比/环比两期对比标准 SQL 样例入库（id 15/16）与知识库口径文档（先按报告期各自聚合再 JOIN，禁止宽表行级 JOIN 后直接 SUM 防笛卡尔放大）；端到端验证 29 机构同比排名与手工 SQL 精确一致（重庆 +3460%、北京 +1052%），全量 473 用例通过 | 系统 |
| 2026-08-17 | v0.4.3 | 快照表跨期重复累计修复（口径配置层）：「各机构本年投放金额排名」等提问生成的 SQL 缺报告期锁定，跨 20 个月末快照直接 SUM，金额虚大近 17 倍（本年投放 2318 亿误算为 39204 亿）。根因为语义指标 filters 不完整（仅 BB=1 无 BBRQ/SJRQ 锁定），且指标层「权威口径禁止自行变更」指令会压制知识库中的正确日期口径。修正：8 个快照表指标（本年/累计投放金额、业务笔数、长龄业务笔数、本年/累计投资收益、逾期金额、风险项目数）filters 统一补报告期锁定（BBRQ/SJRQ=(SELECT MAX(...))）；同步补强知识库 4 个口径 chunk（高频指标口径速查、宽表选表指南金额列段落）。实测问数 SQL 已带 BBRQ 锁定，29 机构合计 2318.28 亿与最新快照精确一致 | 系统 |
| 2026-08-17 | v0.4.2 | 图表同/环比维度错配修复：机构分布等分类维度图表的同比/环比百分比错误（将「前一机构/往前第 N 个机构」的值误当历史基线，产生看似真实实则维度错配的伪数据）。新增时间序列轴判定 detectTemporalAxis（年/年月/年月日/年季/中文年月/裸月等格式 + 单调性校验，全样本可解析且按序排列才视为时间轴），仅时间序列 x 轴启用同/环比对比，分类维度按钮禁用并提示「仅时间序列维度支持对比」；报表全局对比模式下分类图自动回退原值。新增 13 个单测（含机构维度回归用例），全量 46 文件/472 用例通过 | 系统 |
| 2026-08-16 | v0.4.1 | 外部知识库接入 + 交互修复：① 新增外部知识源接入能力（external_kb_sources 表，接口配置仅 ADMIN）：POST 检索协议（query/topK）+ 无/Bearer 认证（API Key AES-256-GCM 加密落库不出明文）+ 响应容错解析（results/documents/data/items 四容器，字段别名兼容）；② 问数链路知识检索阶段并行接入外部源（「外部·源名」前缀注入阶段一 prompt，独立 1200 token 预算，单源失败降级不阻断，trace 记录成功/失败源数）；知识库面板新增「外部知识源接入」管理卡片（接入/编辑/删除/连通测试/生效范围/启用开关，仅 ADMIN 可见）；③ 修复数据源操作按钮悬停提示气泡被左侧导航栏遮挡（固定宽度自动换行）；④ 修复 conversationHistory 测试 10% 随机 flaky（抽样窗口清理压制） | 系统 |
| 2026-08-15 | v0.4.0 | 对话历史管理与自主学习增强：① 对话历史服务端落库（conversation_history 表），问数成功/降级/演示四条路径自动留痕，跨设备共享；② 问数页新增「历史对话」面板（关键词搜索问题/结论、一键重问、单条删除、状态/演示标签）与「导出」按钮（当前源对话导出 Markdown）；③ 自主学习之个人沉淀：本人同数据源真实成功问答对自动作为个人 few-shot 注入后续问数（与团队样例库互补，DAIL-SQL 同口径打分）；④ 自主学习之点踩反例：点踩问答对以「反面教材」注入阶段一 prompt，避免重复同类错误；⑤ 新增 GET/DELETE /api/conversations 接口（仅本人记录，越权删除 404），落库失败记日志不阻断主链路 | 系统 |
| 2026-08-15 | v0.3.10 | 血缘视图悬停横幅显示修复：链路依赖分析叠层加 z-20 提升绘制层级（修复被后续 relative 网格容器遮挡导致内容显示不全），加 pointer-events-none 鼠标穿透防悬停闪烁；链路条改 flex-wrap 随宽度自动换行、高度随内容动态调整，窄屏不再横向滚动裁切；底部归因详情叠层同步加固 | 系统 |
| 2026-08-15 | v0.3.9 | 对话历史按数据源隔离：消息落库时盖归属源戳（提交时快照，加载期间切源不串源），问数页仅渲染当前数据源的对话；多轮上下文仅取当前源历史；「重置对话」仅清空当前源；当前源无历史时动态生成跟随该源表结构的欢迎语；前端存储升级 v4，旧未归属消息迁移至最后活跃数据源 | 系统 |
| 2026-08-14 | v0.3.8 | 数据血缘视图上游归属修正：默认固化监控图表（不良资产宽表 5 图）关联至「数据资源」库（fct_jc_* 宽表），不再错误回退首个数据源；前端存储升级 v3，旧本地缓存经 migrate 自动补全 dataSourceId；字段缺失时血缘视图按最新数据资源库（已注册 Schema 宽表特征匹配）推演上游 | 系统 |
| 2026-08-14 | v0.3.7 | 技能选择交互参照 Qoder IDE「+」方式：移除输入框下方常驻可折叠技能列表，改为输入框左侧「+」按钮，点击向上弹出技能选择面板（技能名称+描述滚动列表，选中后填充提问模板并聚焦输入框，点击面板外部自动关闭），面板底部保留技能库管理入口，输入区更简洁 | 系统 |
| 2026-08-14 | v0.3.6 | 问数链路性能优化（同题实测 105s→68s，重复提问命中结果缓存秒回）：① 复杂度评估启发式预门控（无去重/清洗类信号直接判 simple，省去一次 LLM 评估约 6.8s，ASSESS_ALWAYS_LLM=1 可回退，深度分析开关强制评估不受影响）；② 上下文构建并行化（few-shot/知识库 RAG/语义指标 Promise.all）；③ embedding 短 TTL 缓存（10min/256 条，圈表精排与知识检索复用同一向量）；④ 阶段二解读新增快速模型路由 LLM_ANALYSIS_ENGINE/LLM_ANALYSIS_MODEL（实测 qwen3.7-flash 使解读 83s→30s，未配置时用主模型保质量）；⑤ Ollama chat/embeddings 请求增加 keep_alive=30m 避免模型反复装卸载 | 系统 |
| 2026-08-14 | v0.3.5 | 技能展示交互优化：分析技能从输入框上方横排标签改为输入框下方可折叠列表（默认展开，逐行展示技能名称与描述，点击填充提问模板）；技能库管理入口移至列表标题栏；原位置仅保留计划模式/深度分析/模型自选等模式选项 | 系统 |
| 2026-08-14 | v0.3.4 | 决策报表与看板默认内容专业化：看板默认组件替换为不良资产宽表 2026-08-31 月末快照（核算版）真实数据 5 图（逐月投放趋势/业务分类结构/机构投放 TOP10/长龄机构 TOP8/投资收益走势）；内置示例简报改为不良资产经营分析口径（含真实 KPI 与洞察）；报表模板重构为 4 大不良资产场景（综合经营/资产质量与风险/投资收益与财务/高管战略）；离线降级简报同步切换场景；前端存储版本升至 v2 废弃旧演示缓存 | 系统 |
| 2026-08-14 | v0.3.3 | 数据库性能与问数体验专业化：宽表索引优化（业务表 BB+标志列/维度组合索引、财务表 SJRQ/YJFL 点过滤索引，长龄机构查询 4×、最新快照聚合 20×+ 提速，修正 BB 前缀索引误用导致的全量聚合回表变慢）；系统技能库重构为不良资产经营分析 8 大专业场景（机构画像/长龄/风险/逾期/收益/趋势/结构/存量新增）；新增数据源级专业快速问题推荐（quickQuestions，管理员经 PUT /api/datasources/:id 维护，优先于通用 Schema 推导）；财务宽表 businessNote 补充聚合性能口径；OLLAMA_TIMEOUT_MS 调至 300s | 系统 |
| 2026-08-14 | v0.3.2 | 知识库面向 NL2SQL 优化：新增 3 篇口径/指南类知识文档与 8 个语义指标（BB 版本、月末快照、长龄等口径固化）；检索升级（TOP4 + 单文档限 2 块 + 口径/指南类保留槽位，向量分×词法分混合选块）；embedding 增加 nomic 查询/文档指令前缀并重算存量向量 | 系统 |
| 2026-08-14 | v0.3.1 | 展示字段全面中文化：下钻明细表头返回中文映射（/api/query/drill 新增 columnNames）、降级演示数据补齐中文表头、表名纠偏（LLM 臆加 tbl_/t_ 前缀时确定性还原真实表名，恢复宽表数据源真实执行链路） | 系统 |
| 2026-08-13 | v0.3 | P0-P2 企业级优化：NL2SQL 评测集+评测运行器（server/eval，16 用例）、StateStore 抽象+Redis 外置（REDIS_URL）、语义指标层（/api/metrics）、快速模型路由（LLM_SQL_ENGINE/LLM_SQL_MODEL）、行级权限（scope.rowFilters AST 强制注入）、报表图表点击下钻（/api/query/drill）；修复 validateSelectSql 字符串字面量丢失缺陷 | 系统 |
| 2026-08-13 | v0.2 | 分析过程增强五项：推导过程记录与可视化（query_trace + 时间线回放）、问数计划模式（批准执行）、中间表清洗链（ait_* + 注册表 + TTL 清理）、报表计划模式、报告 PPT 下载（pptxgenjs）；主引擎描述校正为 Ollama | 系统 |
| 2026-08-13 | v0.1 | Qwen 引擎正式启用：主引擎切换为 Qwen qwen3.8-max（百炼标准按量 Key + 通用端点） | 系统 |
| 2026-08-13 | v0.1 | 新增用户自选模型：模型目录接口 + 问数输入框模型下拉（请求级引擎切换，缓存键隔离） | 系统 |
| 2026-08-13 | v0.1 | 新增通义千问百炼引擎支持（AI_ENGINE 三引擎切换 + Coding Plan 专属端点） | 系统 |
| 2026-08-13 | v0.1 | 版本号调整为 NL2SQL Pro v0.1 | 系统 |
| 2026-08-13 | v0.1 | 系统名称调整为「智能问数分析系统」（含网页 title 与登录页/顶栏标题） | 系统 |
| 2026-08-13 | v0.1 | 初始版功能说明书；新增系统内帮助功能（读取本文档实时展示） | 系统 |
